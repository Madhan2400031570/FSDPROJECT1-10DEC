git init
git config - global user. name "MADHAN-18"
git config - global user. Email "2400031570@kl university.in"
git add.
git commit -m "First project"
git branch -M main 
git remotes add origin "https://github.com/username/repositoryname.git
git push -u origin main
git checkout
git log - project history along with Author, commit Msg and time.
